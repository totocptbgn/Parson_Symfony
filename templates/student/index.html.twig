<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Home Student</title>
</head>
<body>
    <nav class="navbar d-flex align-items-center navbar-dark bg-dark fixed-top">
        <span class="navbar-brand mr-auto p-2 text-monospace">Parson</span>
        <div id="info-user" class="text-white-50 p-2" style="margin-right: 10px">Welcome, {{ username }}.</div>
        <a class="btn btn-outline-danger my-2 my-sm-0" style="margin-right: 10px" href="/logout">Log out</a>
    </nav> 

    <div class="container" style="margin-top: 100px;">
        <h1>Choose a course</h1>
        <br>
        {% if courses|length == 0%}
        <div class="alert alert-info" role="alert">
            No courses has been created yet... Come back later !
        </div>
        {% else %}
            <ul class="list-group">
            {% for i in range(0, courses|length - 1) %}
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <a href="/student/course/{{courses[i].id}}" class="text-decoration-none font-weight-light text-body">{{courses[i].title}}</a>
                    {% if state[i] == 2 %}
                        <span class="badge badge-info badge-pill">Empty</span>
                    {% elseif state[i] == 3 %}
                        <span class="badge badge-success badge-pill">Complete</span>
                    {% endif %}
                </li>
            {% endfor %}
            </ul>
        {% endif %}
    </div>
    
</body>
</html>
