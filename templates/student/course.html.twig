<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Course n°{{id}}</title>
</head>
<body>
    <nav class="navbar d-flex align-items-center navbar-dark bg-dark fixed-top">
        <span class="navbar-brand mr-auto p-2 text-monospace">Parson</span>
        <div id="info-user" class="text-white-50 p-2" style="margin-right: 10px">Connected as {{ username }}.</div>
        <a class="btn btn-outline-light my-2 my-sm-0" style="margin-right: 10px" href="/student/home">Home</a>
        <a class="btn btn-outline-danger my-2 my-sm-0" style="margin-right: 10px" href="/logout">Log out</a>
    </nav> 
    
    <div class="container" style="margin-top: 100px;">
        
        {% if no_access %}
            <div class="alert alert-warning" role="alert">
            Sorry {{username}}, but this course does not exist. Come <a href="/student/home" class="alert-link">back</a>.
            </div>
        {% else %}
            <h1>{{course.title}}</h1>
            <p>by <span class="font-weight-bold">{{course.teacher}}</span></p>
            <br>
            {% if exos|length == 0 %}
                <div class="alert alert-info" role="alert">
                   This course is empty, no exercice were added yet. Come back later !
                </div>
            {% else %}
                <ul class="list-group">
                {% for i in range(0, exos|length - 1) %}
                    <li class="list-group-item d-flex justify-content-between align-items-center font-weight-light">
                        <a href="/student/exo/{{exos[i].id}}" class="text-decoration-none font-weight-light text-body">{{exos[i].title}}</a>
                        {% if att[i] %}
                            <span class="badge badge-success badge-pill">Complete</span>
                        {% endif %}
                    </li>
                {% endfor %}
                </ul>
            {% endif %}
        {% endif %}

    </div>
</body>
</html>
